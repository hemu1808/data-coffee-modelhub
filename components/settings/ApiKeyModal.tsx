'use client';

// ─── BYOK MODE DISABLED FOR DEMO ───
// The BYOK key entry UI is disabled while we focus on RAG mode.
// To re-enable: restore the full component body from the _ApiKeyModal_BYOK.tsx.bak
// backup, or uncomment when the Azure backend Orchestrator is built.

// Preserving imports so TypeScript doesn't flag unused-module errors on dependents.
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import React from 'react';

export function ApiKeyModal() {
  return null;
}
